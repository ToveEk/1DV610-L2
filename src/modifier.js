// Dice roll modifiers (ex: +2, -1)
// Kopplas ihop med parser.js och rules.js?

/**
 * Class representing a modifier for dice rolls.
 */
export class Modifier {
  /**
   * Adds a specific number from a roll.
   *
   * @param modifier
   */
  addToRoll (modifier) {
    // add a number to the dice roll
  }

  /**
   * Removes a specific number from a roll.
   *
   * @param modifier
   */
  removeFromRoll (modifier) {

  }
}
